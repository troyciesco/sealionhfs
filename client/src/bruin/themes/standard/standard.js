import { borderRadius } from "./borders"
import { buttonSizes, buttonLevels } from "./buttons"
import { colors } from "./colors"
import { elevations } from "./elevations"
import { textSizes, headingSizes, headingFontFamily, textFontFamily, fontWeight } from "./fonts"
import { spaces, mqMin } from "./layout"

export const standard = {
	borderRadius,
	buttonSizes,
	buttonLevels,
	colors,
	elevations,
	headingFontFamily,
	textFontFamily,
	fontWeight,
	textSizes,
	headingSizes,
	spaces,
	mqMin
}

export { GlobalStyle } from "./GlobalStyle"
